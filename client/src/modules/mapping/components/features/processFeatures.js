export default function processFeatures(vectorLayer, options) {
  // WAS possible properties of options are: propertyequivtoshorttext, HideUnhide, doImages, doHTMLs, setfeatureid, layerindex, setOnMFLTo, setModifiableFeatures, textForSearch
  const {
    propertyEquivToShorttext,
    hideUnhide,
    doImages,
    doHtmls,
    setFeatureid,
    layerIndex,
    setOnMflTo,
    setModifiableFeatures,
    textForSearch,
  } = options;
}
